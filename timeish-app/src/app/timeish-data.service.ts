import { Injectable } from '@angular/core';
import { TimeSheet } from './time-sheet';

@Injectable({
  providedIn: 'root'
})
export class TimeishDataService {
  // Inject end points??
  // Business Logic service for the app
  constructor() { }
  
  // No Idea what i'm doing here, this file should probably be deleted
  // Add methods to call API endpoints
}
